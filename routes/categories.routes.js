const {Router} = require("express");
const controller = require("../controllers/translations.controller.js");
const middleware = require("../middlewares/admin.middleware.js");
const router = Router();

module.exports = router;
